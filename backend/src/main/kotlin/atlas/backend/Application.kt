package atlas.backend

/**
 * Backend entry point. The actual Ktor server wiring lands in M1.7;
 * for now this just confirms the module builds and runs.
 */
fun main() {
    println("netatlas backend starting up...")
}
